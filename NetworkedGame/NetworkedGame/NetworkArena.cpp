#include "NetworkArena.h"


NetworkArena::NetworkArena(Application & app)
	: ArenaState(app)
{
	//Establish connection to server

	//Ping server for player order 
	//server should incremenet its player count per connection

	//
}

NetworkArena::~NetworkArena()
{
}
